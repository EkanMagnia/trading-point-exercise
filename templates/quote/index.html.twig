{%  extends 'base.html.twig' %}

{% block body %}
    <h1>Quotes</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Search') }}</button>
    {{ form_end(form) }}

{% endblock %}